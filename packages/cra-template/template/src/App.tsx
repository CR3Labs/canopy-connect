import { CanopyConnectButton } from 'canopy-connect';

function App() {
  return (
    <div
      style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100vh',
      }}
    >
      <CanopyConnectButton />
    </div>
  );
}

export default App;
